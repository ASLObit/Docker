<odoo>
  <!-- Ocultar pestañas Ventas e Inventario en product.template -->
  <record id="view_product_template_hide_tabs" model="ir.ui.view">
    <field name="name">product.template.hide.tabs</field>
    <field name="model">product.template</field>
    <field name="inherit_id" ref="product.product_template_only_form_view"/>
    <field name="arch" type="xml">

      <!-- Por name -->
      <xpath expr="//page[@name='sales']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//page[@name='inventory']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>

      <!-- Plan B por string (algunas localizaciones) -->
      <xpath expr="//page[@string='Ventas']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//page[@string='Inventario']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>

      <!-- IMPORTANTE: quitamos los xpaths a los stat buttons para evitar el crash.
           Los ocultamos vía CSS en assets. -->
    </field>
  </record>
</odoo>
