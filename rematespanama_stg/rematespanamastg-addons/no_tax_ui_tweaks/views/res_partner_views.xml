<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <!-- Renombrar "Contacto" a "Dirección" y ocultar campos -->
  <record id="view_partner_form_ui_clean" model="ir.ui.view">
    <field name="name">res.partner.ui.clean</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="priority">50</field>
    <field name="arch" type="xml">
      <!-- El grupo de la izquierda normalmente tiene string="Contact" -->
      <group position="attributes">
        <attribute name="string">Dirección</attribute>
      </group>

      <!-- Ocultar campos no usados -->
      <field name="function" position="replace">
        <field name="function" invisible="1"/>
      </field>
      <field name="website" position="replace">
        <field name="website" invisible="1"/>
      </field>
      <field name="title" position="replace">
        <field name="title" invisible="1"/>
      </field>
      <field name="category_id" position="replace">
        <field name="category_id" invisible="1"/>
      </field>
    </field>
  </record>
</odoo>
